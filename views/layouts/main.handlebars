<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ slug }} &mdash; {{ title }}</title>
    <link rel="stylesheet" href="/static/vendor/primer-css/css/primer.css">
    <link rel="stylesheet" href="/static/vendor/octicons/octicons/octicons.css">
    {{{ css 'base' }}}
  </head>
  <body>
    <div class="header">
      <div class="container">
        <h1><a href="/">{{ title }}</a></h1>
      </div>
    </div>
    <div class="container">
      <div class="title">
        <p class="lead">{{ slug }}</p>
      </div>
      {{{ body }}}
    </div>
  </body>
</html>
